@model IEnumerable <Registration.Models.ideaTeam>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idea)
        </th>
        <th></th>
    </tr>
    @*foreach (var item in Model ){}*@
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idea)
            </td>
            <td>
               @* @Html.ActionLink("Accept", "Accept", new { id = item.id }) |*@
                @Html.ActionLink("team Details", "Details", new { id = item.id }) |
                @*@Html.ActionLink("Reject", "Reject", new { id = item.id })*@
            </td>
        </tr>
    }

</table>