@model Registration.Models.users

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Login.cshtml";
}

<h2>Login</h2>

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Login.cshtml";
}


@using (Html.BeginForm(new { @class = "login100-form validate-form p-b-33 p-t-5" }))
{   <div class="box">
        <div class="wrap-input100 validate-input" data-validate="Enter username">
            <div class="form-group">
                @Html.TextBoxFor(x => x.user_name, new { @placeholder = "User dddname", required = "", @class = "input100" })
                <span class="focus-input100" data-placeholder="&#xe82a;"></span>
            </div>
        </div>
        <div class="wrap-input100 validate-input" data-validate="Enter password">
            <div class="form-group">

                @*@Html.LabelFor(m => m.dbstu.GbaTeamLeader)*@
                @Html.TextBoxFor(x => x.user_password, new { @placeholder = "padddssword", required = "", @class = "input100" })
                <span class="focus-input100" data-placeholder="&#xe80f;"></span>
            </div>
        </div>
        <div class="container-login100-form-btn m-t-32">
            <button class="login100-form-btn">
                Login
            </button>
        </div>
    </div>


}